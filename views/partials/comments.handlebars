<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="meta">
      {{user.username}}<span>posted on : {{format_date created_at}}</span>
    </div>
    <div class="text">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div>